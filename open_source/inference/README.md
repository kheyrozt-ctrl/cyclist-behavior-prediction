# Inference Code

This directory contains runtime code for live prediction.

- `unified_prediction/`: current recommended web launcher and unified runtime.
- `intersection_runtime/`: original real-time intersection inference runtime.

For normal use, start the unified runtime from the repository root:

```powershell
.\start_prediction.bat
```
