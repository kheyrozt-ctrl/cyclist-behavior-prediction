# Pose Backends

Pose-estimation support code used by the inference runtimes.

- `trt_pose/`: Jetson/TRT pose backend support and model metadata.

The unified runtime also contains a self-contained copy of the pose backend
under `../inference/unified_prediction/` for portability.
